import { NextPage } from "next";
import CachedData from "../src/external/cache/blog.json";

const Search: NextPage = () => {
  // console.log(CachedData);
  return <div>검색기능 추가중</div>;
};

export default Search;
